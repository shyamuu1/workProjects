export class Data {
        public id:number;
        public fullname:string;
        public initials:string;
        public title:string;
        constructor(id, fullname, initials, title){
                this.id = id;
                this.fullname =fullname;
                this.initials = initials;
                this.title = title;
        }
}
